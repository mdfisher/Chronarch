# Application
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# Database (PostgreSQL + TimescaleDB)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=chronarch
POSTGRES_PASSWORD=chronarch
POSTGRES_DB=chronarch

# Binance API (optional - public endpoints don't require keys)
BINANCE_BASE_URL=https://api.binance.com
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Data ingestion
DEFAULT_SYMBOL=BTCUSDT
HISTORICAL_DAYS=365

# ML Model
MODEL_DIR=./models
PREDICTION_HORIZON_HOURS=1
DIRECTION_THRESHOLD=0.001

# API
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["*"]
